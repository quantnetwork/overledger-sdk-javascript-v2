/**
 * @module overledger-bundle
 */

import OverledgerSDK from '@quantnetwork/overledger-core';

/**
 * Main Overledger SDK bundle. Includes all the packages for all supported DLTs.
 *
 *
 */
export default OverledgerSDK;
